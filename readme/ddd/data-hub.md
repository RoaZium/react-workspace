# 데이터 허브 도메인 설계 문서

## 1. 도메인 개요

### 도메인 정의

**데이터 자산의 체계적 관리 및 분류 시스템**

데이터 허브는 다양한 출처에서 유입되는 데이터 자산을 체계적으로 관리하고 분류하기 위한 시스템입니다.

### 핵심 목적

- 데이터 레이크/웨어하우스의 메타데이터 관리
- 데이터 카탈로그 시스템 구축
- 데이터 거버넌스 및 계보 추적
- 다양한 출처의 데이터 자산 통합 관리

---

## 2. 도메인 구조 (3계층 아키텍처)

```
데이터 허브 도메인
├─ DataSource Context (데이터 출처 관리)
│   └─ DataSource Aggregate
├─ Category Context (데이터 분류 관리)
│   └─ Category Aggregate
└─ Resource Context (개별 리소스 관리)
    └─ Resource Aggregate
```

### 2.1 DataSource (데이터 출처 관리)

**목적**: 데이터가 유입되는 출처/소스를 관리

**테이블**: `data_source`

**핵심 속성**:

- 관리자(소유자) 정보: `user_id`
- 출처 식별 정보: `internal_id`, `public_id`, `code`
- 출처 정보: `name`, `description`
- 기술적 사양: `attributes` (프로토콜, 타임아웃 등)
- 관리 정보: `metadata` (계약 정보, 담당 조직 등)
- 상태 관리: `is_active`, `created_at`, `updated_at`

**관계**:

- Category와 1:N 관계

### 2.2 Category (데이터 분류 관리)

**목적**: 데이터를 논리적으로 분류/조직화

**테이블**: `category`

**핵심 속성**:

- 상위 참조: `data_source_internal_id` (FK)
- 카테고리 식별 정보: `internal_id`, `public_id`, `code`
- 분류 정보: `name`, `description`
- 데이터 정합성 정책: `attributes`
- 검수 상태, 문서 링크: `metadata`
- 상태 관리: `is_active`, `created_at`, `updated_at`

**관계**:

- DataSource의 하위 개념 (N:1)
- Resource와 1:N 관계

### 2.3 Resource (개별 리소스 관리)

**목적**: 실제 데이터 자산/리소스를 개별적으로 관리

**테이블**: `resource`

**핵심 속성**:

- 상위 참조: `category_internal_id` (FK, 제약 없음)
- 리소스 식별 정보: `internal_id`, `public_id`, `code`
- 리소스 정보: `name`, `description`
- 정합성 정책: `attributes`
- 관리 메타데이터: `metadata`
- 상태 관리: `is_active`, `created_at`, `updated_at`

**관계**:

- Category의 하위 개념 (N:1)

---

## 3. 레이아웃 구조 설계 (디자인 시스템)

### 3.1 전체 구조

```
┌─────────────────────────────────────────────────┐
│ Header: "Data Hub" (고정)                        │
├──────────┬──────────────────────────────────────┤
│          │                                      │
│ Sidebar  │  Main Content (메뉴 선택에 따라 변경) │
│ (메뉴)   │  - Dashboard                         │
│          │  - DataSource 관리                   │
│          │  - Category 관리                     │
│          │  - Resource 관리                     │
│          │  - 레이아웃 샘플                      │
└──────────┴──────────────────────────────────────┘
```

### 3.2 Sidebar 메뉴 → Main Content 매핑

| Sidebar 메뉴 | Main Content 레이아웃 구성 |
|-------------|---------------------------|
| 📊 Dashboard | Dashboard Grid (Summary Cards + Activities + Chart) |
| 🗂️ DataSource 관리 | Master-Detail Layout (3:7) |
| 📁 Category 관리 | Three Column Layout (2:3:5) - Tree + List + Detail |
| 📄 Resource 관리 | Grid + Drawer - Filter + Cards/Table + Detail |

### 3.3 메뉴별 Main Content 상세 구성

#### Dashboard (메뉴: 📊 Dashboard)
```
├─ Summary Cards (4개: DataSource, Category, Resource, 활성률)
├─ Recent Activities (최근 활동 타임라인)
├─ Resource Distribution Chart (데이터 분포)
└─ Quick Links (자주 쓰는 링크)
```

#### DataSource 관리 (메뉴: 🗂️ DataSource 관리)
```
├─ Filter Bar (상단 고정)
│   ├─ 검색창
│   └─ 필터 (상태, 생성일)
└─ Master-Detail Layout (3:7)
    ├─ Master Panel (좌측 30%)
    │   └─ DataSource 목록 (테이블)
    └─ Detail Panel (우측 70%)
        ├─ 기본 정보 폼
        ├─ Attributes 편집 (JSON Editor)
        ├─ Metadata 편집 (JSON Editor)
        └─ 연결된 Categories 목록
```

#### Category 관리 (메뉴: 📁 Category 관리)
```
├─ Filter Bar (상단 고정)
│   ├─ 검색창
│   └─ 필터 (DataSource, 상태)
└─ Three Column Layout (2:3:5)
    ├─ Tree Panel (좌측 20%)
    │   └─ DataSource/Category 트리
    ├─ Master Panel (중앙 30%)
    │   └─ 선택된 Category 목록
    └─ Detail Panel (우측 50%)
        ├─ Category 정보 폼
        ├─ Attributes 편집
        └─ 하위 Resources 목록
```

#### Resource 관리 (메뉴: 📄 Resource 관리)
```
├─ Filter Bar (상단 고정)
│   ├─ 검색창
│   └─ 필터 (DataSource, Category, 상태)
└─ Grid + Drawer
    ├─ Grid/Table (메인 영역)
    │   └─ Resource 카드 또는 테이블
    └─ Drawer (우측 슬라이드, 40%)
        ├─ Resource 상세 정보
        ├─ 브레드크럼 (DataSource > Category > Resource)
        └─ 액션 버튼 (편집, 삭제, 즐겨찾기)
```

### 3.4 레이아웃 토큰

```typescript
const LayoutConfig = {
  sidebar: {
    width: 240,
    collapsedWidth: 64,
  },
  header: {
    height: 64,
  },
  spacing: {
    page: 24,      // 페이지 패딩
    section: 16,   // 섹션 간격
    component: 8,  // 컴포넌트 간격
  },
  ratio: {
    masterDetail: [3, 7],
    threeColumn: [2, 3, 5],
    drawer: 0.4,  // 화면의 40%
  },
}
```

---
